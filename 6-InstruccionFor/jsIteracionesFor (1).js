function Mostrar()
{
	var contador=10;

	for (;contador>0;)
	{
		contador--;
	}

	document.write(contador);	
}
	/*var contador=0;
	var iteraciones=10;
	var contadorDePar=0;
	var contadorDeImpar=0;

	for(;;)
	{
		
		if (contador%2==0)
		{
			contadorDePar++;
			document.write("<br>" + contadorDePar);
			
		}
		else
		{
			contadorDeImpar++
			document.write("<br><br><br" + contadorDeImpar);

		}

		contador++;

		if (contador == 10)
		{
			break;
		}
	}

	document.write("<br><br>" + contador);
*/
}